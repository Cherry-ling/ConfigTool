# macOS metadata
.DS_Store

# Local browser automation artifacts
.playwright-cli/

# Root application artifacts are versioned so a full project checkout runs on
# either supported desktop platform without a separate download.

# .NET build outputs and optional release archives
Windows/**/bin/
Windows/**/obj/
dist/
